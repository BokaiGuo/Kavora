# KV Cache Observability 二版报告（补充双子图 + 中文图）

## 新增内容：阈值-推荐RPS双子图

已新增并运行脚本：`scripts/plot_threshold_curve.py`，现在会同时输出：

- 总览图：`results/experiments/reuse_local_r5_final2/threshold_recommended_rps_curve.png`
- 双子图：`results/experiments/reuse_local_r5_final2/threshold_recommended_rps_curve_split.png`
- 中文总览图：`results/experiments/reuse_local_r5_final2/threshold_recommended_rps_curve_zh.png`
- 中文双子图：`results/experiments/reuse_local_r5_final2/threshold_recommended_rps_curve_split_zh.png`
- 明细 CSV：`results/experiments/reuse_local_r5_final2/threshold_recommended_rps_curve.csv`
- 明细 JSON：`results/experiments/reuse_local_r5_final2/threshold_recommended_rps_curve.json`

## 复现实验命令（0.65~0.82，步长 0.01）

- 统一命令入口见：`docs/quickstart_local.md`
- 本节只保留图表解读，避免与 `README.md` 重复维护命令。

## 图表（答辩推荐）

### 1) 总览图（两场景同图）

![threshold_curve](results/experiments/reuse_local_r5_final2/threshold_recommended_rps_curve.png)

### 2) 双子图（low/high 分开展示）

![threshold_curve_split](results/experiments/reuse_local_r5_final2/threshold_recommended_rps_curve_split.png)

### 3) 中文总览图（标题/图例/坐标轴中文）

![threshold_curve_zh](results/experiments/reuse_local_r5_final2/threshold_recommended_rps_curve_zh.png)

### 4) 中文双子图（答辩推荐）

![threshold_curve_split_zh](results/experiments/reuse_local_r5_final2/threshold_recommended_rps_curve_split_zh.png)

## 关键读图结论

- baseline 曲线基本是水平线（不受 `min_hit_ratio` 影响）。
- dual 曲线在阈值升高后阶跃下降。
- low_reuse 在约 `0.72` 开始快速坍塌到 0。
- high_reuse 在约 `0.76` 才坍塌到 0，说明高复用场景对严格阈值更鲁棒。

## 新增：SGLang 实跑并排对照（vLLM vs SGLang）

本轮已实际执行：

- SGLang low/high reuse（`repeats=5`）：
  `results/experiments/reuse_local_sglang_r5_final2/summary.json`
- SGLang 阈值扫描（`0.65~0.82, step=0.01`）：
  `results/experiments/reuse_local_sglang_r5_final2/threshold_recommended_rps_curve.csv`

### 并排结果总表（同一套 workload）

| 场景 | 后端 | req/s 均值 | e2e p95(ms) 均值 | hit_ratio 均值 | baseline 推荐RPS | dual 推荐RPS |
|---|---|---:|---:|---:|---:|---:|
| low_reuse | vLLM | 11.850 | 343.455 | 0.000 | 10.748 | 0.000 |
| low_reuse | SGLang | 11.755 | 356.064 | 0.000 | 11.461 | 0.000 |
| high_reuse | vLLM | 12.895 | 314.603 | 0.246 | 11.633 | 0.000 |
| high_reuse | SGLang | 13.692 | 299.448 | 0.000 | 12.499 | 0.000 |

> 说明：`baseline/dual 推荐RPS` 来自阈值曲线 CSV（本报告取扫描区间内一致值展示；当前两后端在该阈值范围内 dual 均为 0）。

### SGLang 阈值曲线图（本轮新增）

![sglang_threshold_curve](results/experiments/reuse_local_sglang_r5_final2/threshold_recommended_rps_curve.png)

![sglang_threshold_curve_split](results/experiments/reuse_local_sglang_r5_final2/threshold_recommended_rps_curve_split.png)

![sglang_threshold_curve_zh](results/experiments/reuse_local_sglang_r5_final2/threshold_recommended_rps_curve_zh.png)

![sglang_threshold_curve_split_zh](results/experiments/reuse_local_sglang_r5_final2/threshold_recommended_rps_curve_split_zh.png)

### 本轮对照结论（简版）

- 在当前参数下，SGLang 的吞吐/延迟表现不弱于 vLLM（尤其 high_reuse 场景）。
- 但 SGLang exporter 侧 `hit_ratio` 仍为 0，导致 dual 策略在阈值扫描上全部收缩到 0，尚不足以做“缓存收益”结论。
- 因此当前“并排结论”更偏性能对照（req/s、p95），缓存语义对照仍需补齐 SGLang 指标映射后再下最终结论。

## 项目定位与下一阶段规划

### 项目定位

本项目当前更适合定义为：**面向 LLM Serving 场景的 AI infra 可观测与实验框架雏形**，而不是成熟的容量规划系统。

其价值在于，已经跑通了从 **后端指标抓取 -> exporter 派生 -> 实验压测 -> 结果汇总 -> 轻量推荐** 的主链路，能够支持：

- vLLM / SGLang 本地后端的统一实验入口
- KV cache 相关指标的抽取与展示
- low/high reuse 场景下的性能与命中率差异观察
- baseline / dual 策略的对比与阈值扫描可视化

但从生产级 AI infra 视角看，当前系统距离“可直接支撑容量决策”的标准仍有差距，主要原因在于：

- 指标语义还需进一步统一，避免 snapshot 与窗口指标混用
- 不同后端的缓存与 block 语义尚未完全对齐
- 容量推荐仍偏启发式，尚未升级为完整容量评估方法学

另外需要明确的是：当前仓库中这批历史实验产物（如 `reuse_local_r5_final2`、`reuse_local_sglang_r5_final2`）在最新 planner 输出中已显式标记 `hit_ratio_source = snapshot_fallback`。这说明它们当前更适合用于方向性判断与方法验证，不宜直接当作严格窗口语义下的最终容量结论。

因此，下一阶段的重点不应继续放在展示层扩展，而应集中把 **指标语义、后端对齐、容量方法学** 这三件事做硬。

### 下一阶段规划

#### 1) 指标语义做硬

目标是统一 **snapshot / window** 两类指标语义，明确 freshness、missing、stale 的判定与消费规则，避免在线观测语义与实验结论语义混淆。

建议推进：

- 明确 exporter 累计快照指标与实验窗口指标的职责边界
- 在 `summary.json`、`baseline_compare.json`、阈值曲线 CSV/JSON 中统一输出质量状态字段
- 对 `0.0`、`null`、`stale` 做严格区分，避免把“明确测得为 0”和“无法形成有效窗口值”混为一谈
- 让 planner 默认优先消费窗口命中率，对旧 schema 或降级语义做显式标记

预期结果是：实验报告、图表和推荐结果都建立在同一套可解释、可复核的数据语义之上。

#### 2) 后端对齐做硬

目标是补齐 vLLM / SGLang 的缓存指标映射与 block 语义，使跨后端对照从“性能对照”升级为“缓存收益对照”。

建议推进：

- 补齐 SGLang 前缀缓存相关计数器与 exporter 派生指标的映射
- 接入 `vllm_obs` block 语义补丁，增强 `hidden_reuse_ready_perc` 等指标的判别力
- 明确不同后端在 `prefix_hit`、`queries`、`reusable blocks`、`free blocks` 等字段上的语义对齐关系
- 在文档和实验产物中标注“可比字段”和“暂不可比字段”，避免过度解读

预期结果是：vLLM 与 SGLang 在相同 workload 下的实验结论具备统一口径，能够更可靠地回答“谁更快”之外的“谁更能吃到缓存收益”。

#### 3) 容量方法学做硬

目标是把当前基于单轮实验结果的启发式推荐，升级为更接近生产实践的容量评估方法学。

建议推进：

- 从固定 workload 的重复实验扩展到 load sweep、并发扫描与参数矩阵实验
- 引入真实流量回放，验证 synthetic reuse 结论在真实请求分布下的迁移性
- 在推荐结果中加入重复实验统计、波动区间和置信区间，而不是只给出单点推荐值
- 把吞吐、成功率、E2E latency 进一步扩展到 TTFT、TPOT、token throughput、queueing 等关键 serving 指标

预期结果是：系统输出的不再只是“某组参数下的推荐 RPS”，而是带有统计稳健性和业务解释能力的容量结论。

### 小结

简言之，本项目的下一阶段目标，不是把它包装成一个“已经完成的容量规划器”，而是把它建设成一个 **指标可信、跨后端可比、方法学可复核** 的 AI infra 评估系统雏形。只有这三层做硬，后续的图表、报告和推荐结论才真正具备生产参考价值。
